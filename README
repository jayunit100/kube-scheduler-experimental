This is a kubernetes executeable scheduler which uses kube as a baseline dependency.

It is meant to be hacked on for development.

glide install # grab all the deps.  note that -v doesn't work because of some conflict with empty dirs in /jmespath/go-jmespath/fuzz/corpus
rm -rf ./vendor/k8s.io/kubernetes/vendor/
go install
